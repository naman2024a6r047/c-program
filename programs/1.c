//WAP to find addition,subtraction,multiplication,and remainder of two numbers
#include<stdio.h>
int main()
{
    int a,b,sum,sub,mult,rem;
 printf("enter two numbers ");
 scanf("%d%d",&a,&b);
 printf("addition of two numbers =%d \n subtraction of two numbers =%d\n multiplication of two numbers =%d\n remainder of two numbers %d ",sum=a+b,sub=a-b,mult=a*b,rem=b%a);


    return 0;
}